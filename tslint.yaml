extends:
  - tslint:recommended
  - tslint-config-prettier
rules:
  no-console: false
  interface-name:
    - true
    - never-prefix
  max-classes-per-file: false
  variable-name:
    options:
      - allow-leading-underscore
  quotemark:
    options: single
